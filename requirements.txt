# GMaps Lead Scraper - Python Dependencies
# Install: pip install -r requirements.txt

# Core
python-dotenv>=1.0.0

# Google Sheets integration
gspread>=5.12.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Apify (Google Maps scraping)
apify-client>=1.6.0

# Website SIREN scraping (headless browser fallback)
playwright>=1.40.0
